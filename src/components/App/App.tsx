import { FC } from 'react';
import AppRouter from 'components/AppRouter/AppRouter';

const App: FC = () => {
  return (
    <div className="app">
      <AppRouter />
    </div>
  );
};

export default App;

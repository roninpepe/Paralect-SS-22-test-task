import { FC } from 'react';
import Start from 'components/Stubs/Start';

const Home: FC = () => {
  return (
    <div className="app__page page">
      <Start />
    </div>
  );
};

export default Home;
